# Creation du deployment
# Connection sur le pod -> print env -> psql $POSTGRES_URL -> SELECT * FROM cars;
# scale a 0 -> On mettre les logs sur le revoker
# scale à 10 -> on tente de réutiliser l'ancien creds et on montre les logs
# scale à 0